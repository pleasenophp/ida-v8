#include "templates.h"

namespace Ida
{
    // static IdaTemplate *idaTemplate;

    void initTemplates(v8::Isolate *isolate)
    {
        // idaTemplate = new IdaTemplate(isolate, add references to bridges here);
        // idaTemplate->init();
    }

    /*
    IdaTemplate *getIdaTemplate()
    {
        return idaTemplate;
    }
    */

    void deleteTemplates()
    {
        // delete idaTemplate;
    }
}  // namespace Ida
